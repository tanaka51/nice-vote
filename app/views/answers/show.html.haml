%p#notice= notice

- if @vote.closed
  投票が締め切られています

%p
  user name:
  = @answer.user_name
%p
  comment:
  = @answer.comment

%table
  %thead
    %tr
      %td 項目名
      %td 追加情報
      %td 回答
  %tbody
    - @vote.items.each do |item|
      %tr
        %td= item.name
        %td= item.addition
        %td= @answer.items.find{|i| i.vote_item_id == item.id}.try(:rank)

= link_to 'Edit', edit_vote_answer_path(@vote, @answer)
\|
= link_to 'Back', vote_answers_path
